export type parseParameters = {
    podcasts;
    language: string;
    country: string;
    position?: number;
};

export type parseFunctions = {
    maskFunction: Function;
    shortener: Function;
    translateRoot: Function;
};
